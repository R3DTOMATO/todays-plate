<<<<<<< HEAD
# 오늘의 식탁 · 한국 친구 베타 v4.5
=======
# 오늘의 식탁 · 한국 친구 베타 v4.6
>>>>>>> 3c6969c (Fix nearby restaurant relevance v4.6)

한국 사용자가 로그인 없이 `식사 방식 → 원하는 식사 → 예산` 세 단계만 선택해 메뉴를 결정하고, 주변 식당·레시피·실제 식사 기록으로 이어갈 수 있는 정적 웹 베타입니다.

## v4.6 핵심 개선

- 메뉴별 전문 업종 기반 주변 식당 검색
- 바베큐 립을 브런치로 확장하지 않도록 수정
- 일반 식사 메뉴에서 카페·디저트 업종 제외
- 식당 결과를 상호명 일치·메뉴 검색 후보·전문점 후보로 구분
- HTML·JavaScript 배포 캐시 무효화

## v4.4 핵심 개선

- 한국형 집밥 우선 추천
- 같은 음식 계열 리롤 차단
- 시간대가 맞지 않는 아침 전용 메뉴 제외
- 결과 3개의 음식 계열 다양성 보장
- 추천 점수 동점 정렬 문제 수정
- `npm run validate:recommendation` 회귀 테스트 추가

## v4에서 바로 개발한 범위

- 추천 진행 상태 자동 저장 및 12시간 내 이어하기
- 최근 3일 식사 메뉴와 현재 세션 거절 메뉴 반복 추천 차단
- 대표 메뉴 1개와 대안 메뉴 최대 2개
- 가격·조리 시간·식사 방식·주요 재료·매운맛·포만감·알레르기 후보 표시
- 9개 거절 이유와 현재 추천 세션 즉시 반영
- 지출·만족도·재선택 의사·메모·사진을 포함한 식사 기록
- 가짜 예시 기록 없는 빈 상태와 사실 기반 기록 요약
- 주변 식당 실행 시점 위치 권한 요청 및 지역·주소·역 이름 직접 입력
- 선택형 분석 동의, 로컬 데이터 다운로드, 전체 데이터 초기화
- 일반 피드백 접수와 오프라인 재전송 큐
- 이벤트·피드백 수집용 서버 API와 베타 지표 리포트

자세한 변경 내용은 `docs/korea-beta-v4.1-release-notes.md`, 테스트 기준은 `docs/beta-v4-acceptance-checklist.md`를 확인하세요.

## 현재 구조

```text
index.html                   정적 앱 화면
css/app.css                  앱 스타일
js/app.js                    추천·기록·분석·피드백 로직
js/config.js                 배포 환경 API 주소
js/config.example.js         로컬 서버 설정 예시
data/                        메뉴·레시피·검색 키워드 데이터
server/kakao-nearby-proxy.mjs
server/beta-report.mjs
server/data/                 실행 후 생성되는 이벤트·피드백 JSONL
docs/
```

## 1. 앱 실행

`file://`로 직접 열면 JSON 데이터 요청이 차단될 수 있으므로 정적 서버를 사용합니다.

```bash
cd todays_plate_korea_beta_v4
python -m http.server 5500
```

브라우저에서 `http://localhost:5500`을 엽니다.

서버 API까지 함께 테스트하려면 `js/config.example.js` 내용을 `js/config.js`에 적용합니다.

## 2. 베타 API 실행

Kakao REST API 키는 브라우저에 넣지 않습니다.

```bash
KAKAO_REST_API_KEY="발급받은_서버용_키" \
ALLOWED_ORIGINS="http://localhost:5500,http://127.0.0.1:5500" \
node server/kakao-nearby-proxy.mjs
```

API 주소:

```text
GET  /api/health
GET  /api/nearby
GET  /api/resolve-location
POST /api/events
POST /api/feedback
```

Kakao 키가 없어도 상태 확인, 분석 이벤트, 피드백 저장은 동작합니다. 주변 식당과 지역 좌표 검색만 사용할 수 없습니다.

## 3. 베타 리포트 확인

```bash
node server/beta-report.mjs > beta-report.json
```

리포트에는 다음 내용이 포함됩니다.

- 추천 시작·완료·결과 확인·메뉴 선택·식사 기록 퍼널
- 주변 식당·레시피·찜·대안 선택 이용량
- 거절 이유 분포
- 오류 이벤트
- 피드백 유형
- 익명 사용자·세션 수

## 개인정보 원칙

- 추천·기록은 로그인 없이 로컬에 저장됩니다.
- 선택 분석은 사용자가 동의한 경우에만 수집합니다.
- 분석 이벤트에는 이름, 연락처, 정밀 위치, 주소, 사진, 식사 메모를 넣지 않습니다.
- 피드백 연락처는 사용자가 직접 입력한 경우에만 피드백 저장소에 포함됩니다.
- 위치는 주변 식당 검색 요청에만 사용하고 분석 이벤트에는 좌표를 남기지 않습니다.
- 알레르기·제외 식재료는 감점이 아니라 추천 후보 제외 조건입니다.

## 현재 의도적으로 제외한 기능

다음은 이번 친구 베타 범위가 아닙니다.

- 카카오·Google·Apple 로그인
- 여러 기기 동기화
- 친구와 같이 고르기
- 공유 링크
- 푸시 알림
- 와디즈 이용권 및 결제

이 기능들은 친구 베타에서 추천 결정 퍼널과 오류를 먼저 검증한 뒤 순서대로 개발합니다.

## 배포 전 필수 작업

- `js/config.js`에 실제 HTTPS API 주소 설정
- Kakao Developers에서 서버 REST API 키 발급 및 기존 노출 키 폐기
- 음식 이미지 사용 권한 검수와 자체 CDN 이전
- 운영 환경 데이터베이스·백업·접근 제어 적용
- 개인정보 처리방침과 실제 보관·삭제 정책 일치 확인
- 모바일 브라우저 및 iOS Safari 권한 흐름 실기기 테스트


## v4.5 핵심 수정
- 집밥 대표 메뉴를 사용자 국가 기준 음식으로 우선
- 정확 메뉴명 → 동일 음식 문화의 가까운 식당 후보 순으로 검색
- 라조기 등 일반 중식 메뉴를 마라 키워드로 잘못 확장하지 않음
